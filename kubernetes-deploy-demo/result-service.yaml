apiVersion: v1
kind: Service
metadata:
  name: r-service
  labels:
    name: r-service
    app: demo-v-app
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    name: r-app-pod
    app: demo-r-app
